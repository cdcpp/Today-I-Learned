🚀 [TID] JPA 영속성 컨텍스트: 1차 캐시와 더티 체킹 (Dirty Checking)
순수 SQL이나 MyBatis를 사용할 때는 데이터를 조회하고 수정할 때마다 직접 쿼리를 작성해 DB와 통신해야 했다. 하지만 JPA의 심장인 영속성 컨텍스트(Persistence Context)를 도입하면, 애플리케이션과 DB 사이에 강력한 '메모리 캐시'이자 '자동화 로봇'이 하나 생기는 것과 같은 패러다임의 전환이 일어난다.

오늘 학습한 영속성 컨텍스트의 핵심 무기 2가지를 실무 관점에서 정리한다.

1. 1차 캐시 (1st Level Cache): DB를 찌르기 전 메모리부터 뒤진다
동작 원리: JPA를 통해 특정 엔티티를 조회(findById)하면, 무작정 실제 DB로 SELECT 쿼리를 날리지 않고 영속성 컨텍스트 내부의 1차 캐시(메모리)를 가장 먼저 탐색한다.

캐시 히트 & 미스: 1차 캐시에 데이터가 존재하면 DB 통신 없이 객체를 즉시 반환한다. 캐시에 없을 때만 실제 DB에 쿼리를 날려 데이터를 가져온 뒤, 그 결과를 1차 캐시에 저장해 둔다.

실무 인사이트: 동일한 트랜잭션 안에서 같은 데이터를 10번 조회하더라도, 실제 DB로 날아가는 SELECT 쿼리는 최초 1번뿐이다. 애플리케이션 레벨에서 극강의 조회 성능 최적화가 발생한다.

2. 더티 체킹 (Dirty Checking): 실무에서 UPDATE 쿼리가 사라지는 마법
동작 원리: 1차 캐시는 데이터를 처음 불러올 때의 최초 상태(스냅샷)를 몰래 복사해서 가지고 있다.

변경 감지: 자바 코드에서 객체의 값만 변경(ex: member.setName("새이름"))하고 update() 메서드 등을 호출하지 않아도, 트랜잭션이 커밋(Commit)되는 순간 JPA가 현재 객체의 상태와 최초 스냅샷을 싹 비교한다.

자동 쿼리 생성: 변경된 부분을 감지하면 JPA가 알아서 UPDATE 쿼리를 뚝딱 만들어 DB에 쏘아 보낸다.

🚨 핵심 주의사항: 더티 체킹은 반드시 트랜잭션(@Transactional) 안에서만 동작한다. 

