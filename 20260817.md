📝 TIL (Today I Learned) - 2026-08-17
🚀 주제: 람다(Lambda)와 스트림(Stream) - 모던 백엔드 개발 패러다임 적응기
💡 오늘 내가 느낀 점 (Insight)
레거시 환경에서는 데이터를 다룰 때 수많은 if문과 for문을 도배하는 것이 당연했다. 하지만 최근 프로젝트들의 트렌드를 뜯어보니 람다와 스트림을 적극적으로 활용하고 있었고, 기존의 전통적인 자바 코드와는 가독성과 설계 패러다임 자체가 확실히 다르다는 것을 체감했다.

데이터베이스에서 쿼리를 짜듯 자바 코드 내에서 데이터를 직관적으로 조작할 수 있다는 점이 매우 흥미로웠고, 프로젝트에 적용할 생각에 기대가 된다.

📚 핵심 요약 (Key Concepts)
1. 람다 표현식 (Lambda Expression)
개념: 기존의 길고 복잡했던 메서드(익명 내부 클래스)를 자바스크립트의 화살표 함수(Arrow Function)처럼 간결한 식(Expression)으로 표현하는 문법.

특징: (파라미터) -> { 로직 } 형태로 작성되며, 코드가 압도적으로 짧아진다.

더 나아가서: Class::method 형태의 메서드 참조(Method Reference)를 사용하면 극한의 간결함을 뽑아낼 수 있다.

2. 스트림 (Stream API)
개념: 배열이나 컬렉션(List, Map 등)의 데이터를 '일회성 컨베이어 벨트(스트림 객체)'에 올려두고, 파이프라인을 지나가며 데이터를 가공 및 반환하는 기술.

특징: 원본 데이터를 훼손하지 않으며(불변성), 마치 DB의 SQL 쿼리(WHERE, SELECT, ORDER BY 등)를 작성하듯 데이터를 필터링하고 조작할 수 있다.

⚙️ 자주 쓰는 스트림 파이프라인 (DB 쿼리와 비교)

filter(): 조건에 맞는 데이터만 통과시킨다. (DB의 WHERE)

map(): 데이터를 원하는 특정 필드나 형태로 변환한다. (DB의 SELECT)

sorted(): 데이터를 기준에 맞춰 정렬한다. (DB의 ORDER BY)

limit(): 지정한 개수만큼만 데이터를 잘라낸다. (DB의 LIMIT)

collect(): 벨트를 통과한 최종 데이터들을 다시 List, Map, 문자열 등으로 깔끔하게 재포장한다.

🔥 Action Item

앞으로 컬렉션 데이터를 다룰 때 습관적인 for문 사용을 멈추고 스트림 파이프라인 도입을 1순위로 고려할 것.

NullPointerException을 방어해 주는 Optional과 스트림을 결합한 안전한 데이터 추출 패턴을 실무에 적극 적용하기.
