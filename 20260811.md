# [20260811 TIL] 자바 Enum과 Annotation 핵심 정리

그동안 무지성으로 쓰던 Enum이랑 Annotation이 내부적으로 어떻게 동작하는지 짚고 넘어감. 

## 1. Enum (열거형)
단순한 상수 모음인 줄 알았는데, 자바에서는 사실상 **객체(클래스)**였다.
예전처럼 `public static final String VIP = "VIP"` 이렇게 쓰면 오타 났을 때 문제가 생기는데, Enum을 쓰면 엉뚱한 값 들어오는 걸 컴파일 타임에 빨간줄로 다 잡아줌 (타입 안정성 확보).

가장 큰 장점은 **상수 자체에 데이터랑 로직을 넣을 수 있다는 것**. 

```java
public enum MemberGrade {
    BASIC(0), VIP(10), VVIP(20); // 괄호 안에 고정 데이터(할인율) 세팅

    private final int discountPercent;

    MemberGrade(int discountPercent) {
        this.discountPercent = discountPercent;
    }

    // Enum 안에서 직접 로직 처리
    public int calculate(int price) {
        return price - (price * discountPercent / 100);
    }
}

2. Annotation & Reflection

Annotation (애너테이션): 얘 자체는 진짜 아무 기능도 없는 그냥 메모장(마커)임.

Reflection (리플렉션): 런타임에 엑스레이처럼 클래스 내부(변수, 메서드 등)를 뜯어볼 수 있는 기술.

즉, 내가 코드에 애너테이션을 달아두면 -> 런타임에 리플렉션이 그걸 스캔해서 읽고 -> 정해진 처리 로직(스프링 등)이 대신 일을 해주는 구조.

// 커스텀 애너테이션 생성
@Target(ElementType.FIELD) // 변수에 붙일 거임
@Retention(RetentionPolicy.RUNTIME) // 런타임까지 살아있게 세팅
public @interface MaxLength {
    int value();
}

// 실제 사용
public class User {
    @MaxLength(value = 5) // 나중에 리플렉션이 읽어갈 타겟
    private String nickname;
}
