# 💡 [Java] 제네릭(Generics) 제대로 알고 쓰기

단순히 '타입 캐스팅을 생략하기 위한 편리한 기능' 정도로 얕게 사용하던 제네릭을, 공통 API 설계와 클린 코드 관점에서 다시 정리해 보았다. 
가장 중요한 핵심은 **'발생할 수 있는 런타임 에러를 컴파일 타임으로 끌어와 미리 차단하는 것'**이다.

## 1. 제네릭의 무공변성 (Invariance)
설계 시 가장 많이 헷갈리는 부분이다. 객체의 상속 관계가 제네릭 컬렉션에서는 그대로 유지되지 않는다.
예를 들어 `Animal`이 `Dog`의 부모라고 해서, `List<Animal>`이 `List<Dog>`의 부모가 되지는 않는다.

```java
// 만약 자바가 아래 코드를 허용했다면?
List<Animal> animals = new ArrayList<Dog>(); 
animals.add(new Cat()); // Dog 리스트에 Cat이 들어간다!!

이러한 런타임 타입 불일치를 원천 차단하기 위해, 자바 컴파일러는 애초에 위 코드에서 즉시 컴파일 에러를 발생시킨다.

2. 와일드카드와 PECS 공식
메서드 파라미터로 제네릭을 받을 때 유연성을 극대화하기 위해 와일드카드(?)를 쓴다.
이때 PECS (Producer Extends, Consumer Super) 원칙을 적용하면 데이터의 오염을 막을 수 있다.

? extends T (Producer): 주로 데이터를 조회 할 때 사용한다. 컬렉션에 데이터를 추가하려 하면 컴파일 에러가 발생하여 안전하다.

? super T (Consumer): 데이터를 추가(Write)할 때 사용한다. 특정 타입과 그 부모 타입만 허용되므로 안전하게 데이터를 넣을 수 있다.

public void addData(List<? super Number> list) {
    list.add(10); // Number의 자식인 Integer(10)를 안전하게 추가 가능
}

3. Bounded Type (타입 제한하기)
공통 응답 객체(Response DTO) 등을 설계할 때, 파라미터 <T>에 아무 타입이나 들어오는 것을 제어해야 할 때가 있다. 이때 extends 키워드로 허용할 타입을 강제할 수 있다.

// BaseDto를 상속받은 타입만 T 자리에 올 수 있도록 강제함
public class ApiResponse<T extends BaseDto> {
    private String code;
    private String message;
    private T data;
    
    // ...
}

이렇게 API 규격을 설계해 두면, 다른 개발자가 실수로 ApiResponse<String>처럼 잘못된 타입을 선언했을 때 컴파일러가 즉시 에러를 잡아내어 휴먼 에러를 방지할 수 있다.

📝 Today's Retrospective
평 사용하던 제네릭의 본질과 '타입 안정성'의 중요성을 깊이 체감한 하루였다. 과거 화면(View) 중심의 개발부터 현재 REST API 설계에 이르기까지, 단순히 데이터를 전달하고 기능을 구현하는 것에 집중하느라 타입 관리를 다소 느슨하게 했던 지난날의 코드들을 돌아보게 되었다.
견고한 시스템은 결국 '개발자의 실수를 컴파일 타임에 얼마나 잘 차단하느냐'에 달려있다. 앞으로 공통 모듈이나 API 응답 객체를 설계할 때, 오늘 정리한 Bounded Type과 PECS 원칙을 적극 활용하여 보다 안전하고 객체지향적인 아키텍처를 고민해야겠다.
