📝 [TIL] 자바 HashMap과 getOrDefault()를 활용한 데이터 집계 최적화
1. 오늘의 훈련 목표
대규모 데이터(예: 10만 건 이상의 배열)에서 특정 요소의 개수를 세거나 중복을 검사할 때 발생하는 시간 초과(Time Limit Exceeded) 병목 현상을 해결한다.

2. HashMap
- 데이터를 순서대로 나열하는 List나 Array와 달리, '키(Key)'와 '값(Value)'의 쌍으로 데이터를 저장하는 자료구조.

- 특정 데이터를 찾을 때 처음부터 끝까지 뒤질 필요 없이(O(N)), '이름표(Key)'를 통해 단 한 번에 데이터에 접근(O(1))할 수 있어 압도적인 검색 속도를 자랑한다.

3. 마법의 메서드: getOrDefault(key, defaultValue)
맵에 데이터를 넣을 때 "기존에 이 데이터가 있었나?"를 확인하는 로직을 단 한 줄로 압축해 주는 핵심 메서드다.

동작 방식: "맵을 뒤져서 이 key가 있으면 원래 있던 값을 꺼내오고, 만약 한 번도 들어온 적 없는 초면인 key라면 내가 지정한 defaultValue를 줘!"

❌ 기존 방식 (if-else 분기)

Java
// 이름이 이미 맵에 있는지 확인하고 분기 처리해야 함
if (map.containsKey(name)) {
    map.put(name, map.get(name) + 1); // 있으면 기존 값 + 1
} else {
    map.put(name, 1); // 없으면 1로 새로 등록
}
✅ getOrDefault를 활용한 우아한 방식

Java
// 단 한 줄로 완벽하게 동일한 로직 수행!
map.put(name, map.getOrDefault(name, 0) + 1);
해석: name이 맵에 없으면 0을 가져와서 1을 더해(0 + 1 = 1) 저장하고, 이미 3번 들어온 이름이면 기존 값 3을 가져와서 1을 더해(3 + 1 = 4) 덮어씌운다.

4. 실무/코테 적용 포인트
수십만 건의 로그 데이터나 유저 행동 데이터를 순회하며 '빈도수'를 카운팅할 때 최적의 성능을 낸다.

불필요한 if-else 블록을 걷어내어 코드가 훨씬 간결해지고 유지보수성이 높아진다.
