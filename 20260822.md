# [TIL] 자바 Record 실무 활용법: Lombok과의 역할 분담 및 확장 기법

그동안 자바에서 데이터 클래스를 만들 때는 습관적으로 Lombok(`@Getter`, `@Setter`, `@Data` 등)을 도배하는 것이 정답인 줄 알았다. 하지만 오늘 자바 14부터 도입된 `Record`를 깊게 파보면서, 데이터 객체를 다루는 관점이 완전히 바뀌었다. 

앞으로는 목적에 맞춰 **Entity는 Lombok**, **DTO/VO는 Record**로 명확하게 역할을 분리해서 써야겠다.

### 1. DTO는 Record, Entity는 Lombok
- **DTO/VO (Record):** 클라이언트와 데이터를 주고받을 때 쓰는 DTO나 값 자체를 표현하는 VO는 배송 도중 데이터가 오염되면 안 된다(불변성). Record는 단 1줄 선언만으로 생성자, Getter, `equals/hashCode`를 모두 만들어주면서 불변성까지 보장해주니 완벽한 DTO 생성기다.
- **Entity (Lombok):** 반면 DB 테이블과 매핑되는 JPA Entity는 프레임워크 특성상 기본 생성자도 필요하고(Proxy 객체 생성 등) 상태 변화도 일어날 수 있기 때문에 기존처럼 Class 구조에 Lombok을 조합하는 것이 맞다.

### 2. Setter가 없는데 값을 갱신하고 싶다면? (Wither 패턴)
Record는 모든 필드가 `private final`로 잠겨 있어서 Setter가 아예 존재하지 않는다. 값이 하나라도 바뀌어야 한다면 원본을 건드리지 않고 '새로운 객체'를 만들어야 한다. 
실무에서는 필드가 많을 때 매번 `new`로 길게 생성하는 수고를 덜기 위해 **Wither 패턴**을 활용한다.

```java
public record MemberDto(String name, int age, String grade) {
    
    // 특정 필드(나이)만 변경된 '새로운' 객체를 반환하는 Wither 메서드
    public MemberDto withAge(int newAge) {
        return new MemberDto(this.name, newAge, this.grade);
    }
}

이렇게 내부에 만들어두면 호출하는 쪽에서 member.withAge(31) 처럼 원본의 불변성은 완벽히 지키면서도 Setter를 쓰는 것처럼 깔끔하게 코드를 작성할 수 있다.

3. 입맛에 맞게 toString() 오버라이딩 (민감 정보 마스킹)
Record가 기본적으로 만들어주는 toString() 출력 포맷도 훌륭하지만, 실무에서는 비밀번호나 주민번호 같은 민감 데이터가 로그에 그대로 찍히는 대참사를 막아야 한다.
이럴 때는 일반 클래스처럼 toString()을 직접 오버라이딩해서 안전하게 커스텀할 수 있다.


```java
public record MemberDto(String name, int age, String password) {
    
    @Override
    public String toString() {
        // 비밀번호는 별표 처리해서 안전하게 로그 출력
        return String.format("Member[name=%s, age=%d, password=****]", name, age);
    }
}

💡 오늘의 3줄 요약:

Lombok 도배는 그만. 불변성이 필요한 DTO와 VO는 Record로도 작성해보자!
값의 변경이 필요할 때는 Setter 대신 Wither 패턴으로 새로운 Record를 반환하자.
민감한 정보가 있다면 toString()을 오버라이딩해서 마스킹 처리하자.
